// Single source of truth for the app version shown on every page.
// Bump this with each release so users (and you) can confirm which
// build a browser is actually running.
export const APP_VERSION = '1.1.6';
